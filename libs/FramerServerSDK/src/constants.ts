// These are related to the base initial path of the _server_ endpoints.
// Ex: `http://localhost:3000/api` and `http://localhost:3000/f`
// NOT the sub paths ----> `http://localhost:3000/api/projects/f` <--- NO DO NOT DO THAT ILLEGAL!

export const FRAMES_SERVER_BASE_PATH = '/f';
export const API_SERVER_BASE_PATH = '/api';
